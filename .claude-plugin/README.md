# .claude-plugin

This directory contains Claude Code plugin metadata files.

## Files

- `plugin.json` - Plugin name, version, author, and keywords
- `marketplace.json` - Marketplace registration for plugin discovery

## Installation

This plugin can be installed in Claude Code via the marketplace or by cloning
the repository and adding it to your plugins directory.
